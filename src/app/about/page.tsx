export default function AboutPage() {
  return (
    <div className="site-page">
      <section className="site-hero site-hero--compact">
        <div className="site-kicker">About</div>
        <h1 className="site-title">A local-first operator, not a fake everything-app</h1>
        <p className="site-lead">
          Elyan is built to stay small, explicit, and shippable: local runtime first, narrow hosted control plane second.
        </p>
      </section>
    </div>
  );
}
