[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
testpaths = tests
norecursedirs =
    .git
    .github
    .venv
    venv
    bot
    artifacts
    logs
    site
python_files = test_*.py
markers =
    smoke: Lightweight end-to-end smoke checks for CI gating.
